definition_version: 2

mixins:
  snowpark_shared:
    stage: "dev_deployment"

entities:
  hello_procedure:
    type: "procedure"
    stage: "stage_a"
    identifier:
      name: "hello_procedure"
    handler: "a.hello_procedure"
    signature:
      - name: "name"
        type: "string"
    returns: string
    artifacts:
      - "app_1/*"
