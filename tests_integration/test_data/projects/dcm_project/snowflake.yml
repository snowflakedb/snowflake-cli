definition_version: 2
entities:
  my_project:
    type: project
    identifier: "my_project"
    stage: "my_project_stage"
    main_file: manifest.yml
    artifacts:
      - file_a.sql
      - file_b.sql
      - manifest.yml
