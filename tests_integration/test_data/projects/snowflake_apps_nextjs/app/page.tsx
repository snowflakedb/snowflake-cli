export const dynamic = "force-dynamic";

export default function Home() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>Snowflake Apps — Next.js integration test</h1>
      <p>Minimal standalone app (see demos/integration-test in snowflake-app-services).</p>
    </main>
  );
}
