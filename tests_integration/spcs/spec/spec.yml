spec:
  containers:
  - name: hello-world
    image: /sichen_db/test_schema/sichen_repo/echo_service:latest
    env:
      SERVER_PORT: 8000
    readinessProbe:
      port: 8000
      path: /healthcheck
  endpoints:
  - name: echoendpoint
    port: 8000
    public: true
