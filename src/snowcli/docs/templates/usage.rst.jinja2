{{ help }}

Syntax
===============================================================================

.. code-block:: console

snow {% for part in path %}{{ part }}{{ " " if not loop.last }}{% endfor %} [OPTIONS]

Arguments
===============================================================================

None

Options
===============================================================================
{% for param in params %}
:samp:`{% for p in param['opts'] %}{{ p }}{{ "," if not loop.last }} {% endfor %} <{{ param["name"] }}>`
{% if param["help"] %}{{ param["help"] }}{% else %}TBD{% endif %}
{% endfor %}
