name: Mirror main to public

on:
  push:
    branches:
      - main
  schedule:
    - cron: '7 * * * *'  # hourly fallback, offset to avoid :00 thundering-herd
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (no actual changes pushed)'
        type: boolean
        default: false

concurrency:
  group: mirror-main
  cancel-in-progress: false

jobs:
  mirror:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - uses: actions/create-github-app-token@v1
        id: mirror-token
        with:
          app-id: ${{ secrets.MIRRORING_APP_ID }}
          private-key: ${{ secrets.MIRRORING_APP_PRIVATE_KEY }}
          owner: snowflakedb
          repositories: snowflake-cli

      - name: Load config
        run: |
          source scripts/mirroring/lib/config.sh
          echo "COPYBARA_RELEASE=${COPYBARA_RELEASE}" >> "$GITHUB_ENV"

      - uses: docker/setup-buildx-action@v3

      - uses: docker/build-push-action@v6
        with:
          context: scripts/mirroring/copybara
          push: false
          load: true
          tags: copybara:local
          build-args: |
            COPYBARA_RELEASE=${{ env.COPYBARA_RELEASE }}

      - name: Run mirror-trunk.sh
        env:
          SOURCE_GH_TOKEN: ${{ github.token }}
          MIRROR_GH_TOKEN: ${{ steps.mirror-token.outputs.token }}
        run: |
          args=()
          [[ "${{ inputs.dry_run }}" == "true" ]] && args+=(--dry-run)
          bash scripts/mirroring/mirror-trunk.sh "${args[@]}"
