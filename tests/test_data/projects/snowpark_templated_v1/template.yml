files_to_render:
  - snowflake.yml
variables:
  - name: project_name
    prompt: "Project identifier (used to determine artifacts stage path)"
    default: my_snowpark_project
    type: string
  - name: stage
    prompt: "What stage should the procedures and functions be deployed to?"
    default: dev_deployment
    type: string
